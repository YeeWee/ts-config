# TypeScript Standards

本文件写**判断类**规则：工具管不到的、以及不同人会写出不同答案的地方。

机械格式（缩进、引号、分号、行宽、import 顺序、命名大小写）由本包附带的配置决定，本文件不重复；有疑问就读配置，冲突时以配置为准。**凡是能机械检查的规则，都应该变成配置里的一条规则，而不是这里的一句话**——留在文字里的规则会随注意力漂移，变成机器规则的会一直生效。

适用对象：任何引用 `@yeewee/ts-config` 的项目。项目专属约定（领域词表、协议边界的处理）写在该项目自己的文档里，并指向本文件。

## 1. 类型边界：外部数据在边缘解析一次

所有外部输入——请求参数、HTTP 响应、配置文件、数据库行、第三方 SDK 返回值——在进入系统的那一层解析成领域类型。解析之后，内部代码只见到已定型的数据。

未定型数据向内渗透，会让每个使用点都要重新防御一遍，这是认知负荷和未知未知的主要来源。

- **类型由校验逻辑推导**，不手写一份并行的 `interface`。校验 schema 是唯一真相，类型用 `z.infer<typeof X>` 一类手法取出来。手写的那份必然漂移，而且漂移时不会有任何报错。
- **内部函数签名里出现 `unknown` 是边界画错的信号**，不是"这里要小心处理"的提示。把解析上移到入口，而不是在下游加判断。
- 校验失败与业务失败分开：格式不对在边界就拒掉，不要带进内部再逐层判断。

## 2. 模块接口：导出面就是接口

深模块的标准是接口窄、实现厚。在 TypeScript 里，接口窄不窄完全由 `export` 决定。

- **默认不导出。** 只有调用方确实需要的东西才 `export`；内部类型、辅助函数留在模块内。每多一个导出就多一个必须维护的承诺。
- **import 写真实文件后缀（`.ts`），不写 `.js`。** 编译时由 `rewriteRelativeImportExtensions` 改写成 `.js`，源码因此能被 `node` 直接运行——跑测试不需要任何 loader。这是与主流惯例相反的选择，理由见 `docs/adr/0002`。
- **不写 barrel（`index.ts` 全量 re-export）。** 需要聚合时按显式清单逐个导出。`dependency-cruiser` 的边界规则从机制上禁止外部代码越过入口文件，barrel 会把这条边界抹平。
- **导出的函数写显式返回类型。** 返回类型是公开契约的一部分；隐式推断会让一次内部重构悄悄改变对外行为。
- **直通模块出现即处理。** 一个文件只是把另一个模块的函数转出去，说明这层抽象不存在——删掉或合并，而不是留着。
- 参数超过三个且总是一起出现，收成一个对象；但不要为想象中的需求加泛型或选项参数。

## 3. 领域类型：把领域约束交给类型系统

- **领域标识符不要都用 `string`。** 语义不同、字面相似的标识符互换后编译通过、运行才出错。用 branded 类型区分：

  ```ts
  type Brand<T, B extends string> = T & { readonly __brand: B }
  type UserId = Brand<string, 'UserId'>
  type TenantId = Brand<string, 'TenantId'>
  ```

  把"传错参数"从运行时 bug 变成编译错误。
- **状态用可辨识联合，不用一组可选字段。** 可选字段的组合允许非法状态存在：

  ```ts
  type SyncState =
    | { status: 'idle' }
    | { status: 'syncing'; startedAt: Date }
    | { status: 'failed'; error: Error }
  ```
- 用字符串字面量联合 + `as const` 对象代替 `enum`：`enum` 有运行时产物、不参与摇树、跨包边界行为不一致。这条已由 ESLint 强制（`no-restricted-syntax`），不用靠记性。
- 领域名词用项目自己 `CONTEXT.md` 词表里的词，不在代码里另造同义词。

## 4. 错误与异步

- **错误策略全项目统一，不混用。** 进程内部抛类型化错误（继承 `Error` 并带机器可读的 `code` 字段）；跨进程/协议边界时转成结构化结果，不把异常和栈泄漏给调用方。
- 空的 `catch` 块必须写明为什么可以忽略；写不出理由就说明不该吞。
- 不留下悬空 promise。并行用 `Promise.all`；有先后依赖的不要为了"看起来并行"提前发起。
- 不要用 `void promise` 消掉 lint 警告——那是把错误藏起来。

## 5. 测试

- 测试 fixture 不用 `as` 强转成完整类型，用 partial 构造。`as` 会随类型演进悄悄变错，而编译器不再替你检查。
- 断言公开接口的行为，不测私有实现细节。
- 测试名里的领域概念同样用项目 `CONTEXT.md` 的词表。
